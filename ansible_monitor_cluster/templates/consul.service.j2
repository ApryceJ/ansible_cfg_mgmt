[Unit]
Description=Consul Startup process
After=network.target

[Service]
Type=simple
ExecStart=/bin/bash -c '/usr/bin/consul agent -data-dir="{{ data_dir }}" -config-dir=/etc/consul.d/config.jason -bind {{ ansible_eth1.ipv4['address'] }}'
TimeoutStartSec=0

[Install]
WantedBy=default.target
